"use strict";

function обычноОбъявленнаяФункция() {
    console.log(this.name);
}

const стрелочнаяФункция = () => {
    console.log(this.name);
};

const obj = {
    name: 'Вася',
    обычноОбъявленнаяФункция,
    стрелочнаяФункция,
}

obj.обычноОбъявленнаяФункция() // 'Вася'
obj.стрелочнаяФункция() // ''

1 || 2 || 0 // 1
2 && 0 && 3 // 0

1 > 2 // false / true
({}) == ([]) // 

NaN undefined null +0 -0 == === ({}) == 4 ([]) == 2